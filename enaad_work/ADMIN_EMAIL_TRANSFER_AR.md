# نقل حساب الإدارة والبريد الاحتياطي

تم نقل حساب الإدارة الوحيد من `enaadx@gmail.com` إلى `enaad4786@gmail.com`.

عند أول نشر بعد تطبيق هذا التعديل:

- يُفعّل `enaad4786@gmail.com` في قائمة `SUPER_ADMIN`.
- إذا كان الحساب الجديد موجودًا، يصبح دوره `admin` ويُفعّل توثيق بريده.
- يُعطّل البريد القديم داخل `adminAllowlist` ويعود حسابه إلى دور `user` إن كان موجودًا.
- تُرسل النسخ الاحتياطية إلى البريد الجديد لأن المستلم يعتمد على سياسة حساب الإدارة الوحيد.

## Resend

لا تُحفظ مفاتيح Resend داخل المشروع. أنشئ مفتاحًا جديدًا من حساب Resend المرتبط بالبريد الجديد، ثم استبدل قيمة `RESEND_API_KEY` داخل Vercel، وبعدها أنشئ Deployment جديدًا. لا تحذف المفتاح القديم قبل نجاح اختبار الإرسال بالمفتاح الجديد.
